from pair import Pair, nil


def tokenize(expression):
    return expression.replace("(","( ").replace(")"," )").split()

def parse_tokens(tokens, index):
    """ my doctests
    >>> parse_tokens(tokenize('(* (- 8 4) 4)') , 0)
    Pair('*', Pair(Pair('-', Pair(8, Pair(4, nil))), Pair(4, nil)))
    """
    if cond 1
        
    elif cond 2
        
    else:
        try:
            pass


        except:
            raise TypeError
    pass